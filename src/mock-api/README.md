# Mock API Layer

Reserved for API adapter functions.

Current implementation reads directly from `src/data/mockData.ts`.
Future backend integration should add request clients and mapper functions here.
